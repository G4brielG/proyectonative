import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#BFC6FE',
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
  texto: {
    color: '#FFFFFF'
  },
  botonAgregar: {
    alignItems: 'center',
    backgroundColor: 950,
    borderRadius: 30,
    bottom: -20,
    height: 60,
    justifyContent: 'center',
    position: 'absolute'
  }
});

export default styles